# Layer 3 — View

Thin UI that reads `GameState` and dispatches actions. It computes **nothing** about
game rules — that is the engine/view wall, the one boundary this project cannot
violate.

Empty for now — there is intentionally no view code yet.
