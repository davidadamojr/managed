# Layer 2 — Content / Data

Data files only: skills, name lists, events, tuning constants, stubbed archetypes.
**No logic.** The engine (Layer 1) consumes this data; this layer never imports the
engine.

Empty for now.
